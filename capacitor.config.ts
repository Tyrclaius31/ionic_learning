import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ejercicio_2_ng',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
